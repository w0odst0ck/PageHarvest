# 🛑 DO NOT TOUCH — 原始代码红线

以下目录是**原始封装代码**，**严禁修改**：

- `selection/`       — 选品分析逻辑（picker 策略、分类阈值）
- `platforms/`       — 各平台解析器（搜索/详情适配器）
- `core/`            — 核心注册表、Schema
- `gap/`             — 缺口分析模块
- `pipeline/`        — 旧流水线脚本

## 可以动的

- `api/`             — 新产品化层（子进程调底层，不改逻辑）
- `streamlit_app/`   — 前端展示
- `data/`            — 数据文件
- `memory/`          — 项目日志

## 防护措施

1. **Git skip-worktree** — 修改也不会被提交
2. **文件系统只读** (`chmod a-w`) — 物理无法写入
