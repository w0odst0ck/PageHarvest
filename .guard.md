# 🛑 DO NOT TOUCH — 原始代码红线

以下目录是**原始封装代码**，**严禁修改**：

- `src/core/`            — 核心注册表、Schema
- `src/platforms/`       — 各平台解析器（搜索/详情适配器）
- `src/selection/`       — 选品分析逻辑（picker 策略、分类阈值）
- `src/gap/`             — 缺口分析模块
- `src/pipeline/`        — 旧流水线脚本

## 可以动的

- `src/api/`             — 新产品化层
- `src/web/`             — Flask Web 应用
- `src/cli/`             — 离线/在线 CLI 入口
- `modules/factory-monitor/` — 工厂监控模块
- `modules/price-compare/`   — 询价比价模块
- `modules/monitor/`     — 价格监控（未开始）
- `site/`                — GitHub Pages 站点
- `docs/`                — 规划文档 + 项目日志

## 防护措施

1. **Git skip-worktree** — 修改也不会被提交
2. **文件系统只读** (`chmod a-w`) — 物理无法写入
